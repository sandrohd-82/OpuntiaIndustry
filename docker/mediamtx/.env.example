# Copia in .env in questa cartella (non committare).
# Token: Cloudflare Zero Trust → Networks → Tunnels → il tunnel → Install connector.
TUNNEL_TOKEN=

# IP LAN del PC officina (solo WebRTC locale, non serve al Tunnel HLS)
MEDIAMTX_LAN_IP=192.168.1.10
